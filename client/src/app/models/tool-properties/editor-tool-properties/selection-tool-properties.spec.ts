import { SelectionToolProperties } from './selection-tool-properties';

describe('SelectionToolProperties', () => {
  it('should create an instance', () => {
    expect(new SelectionToolProperties()).toBeTruthy();
  });
});
